name: Amazon Job Monitor

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Check repository
        uses: actions/checkout@v4

      - name: Run monitor
        run: |
          echo "Amazon Bolton job monitor started"
          echo "Browser monitoring will be added next."
